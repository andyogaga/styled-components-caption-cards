import React from 'react'
import { Navbar, LogoContainer, MenuContainer } from '.'

const NavBar = props => {
  return (
    <Navbar >
      <LogoContainer />
      <MenuContainer />
    </Navbar>
  )
}

export default NavBar
